<Window x:Class="Practice_18.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Practice_18"
        mc:Ignorable="d"
        Title="Animal info" Height="250" Width="400">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <!--<TextBox x:Name="tbAnimalType" />-->
        <ComboBox x:Name="cbAnimalType" IsEditable="True" />
        <TextBox x:Name="tbAnimalName" Grid.Row="1" />
        <TextBox x:Name="tbAnimalInfo" Grid.Row="2" />
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center">
            <Button x:Name="btnOk" Content="OK" Width="100" />
            <Button x:Name="btnCancel" Content="Cancel" Width="100" />
        </StackPanel>
    </Grid>
</Window>
